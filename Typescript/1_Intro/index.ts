const firstName = "Rafael";
const anotherNome = "1";

function greating(name : string) {
    console.log("Ola " + name);

}

// sempre estar atento ao tipo da variavel criadar pois só sera executada quando for do tipo esperrado.


greating(firstName)
greating(anotherNome)
